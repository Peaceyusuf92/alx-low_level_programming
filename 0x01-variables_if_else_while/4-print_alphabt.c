#include <stdio.h>
/**
 * main - main block
 * Description: alphabets except q and e
 * Return: 0
 */
int main(void)
{
	char x = 'a';

	while (x <= 'z')
	{
		if (c != 'e' && c != 'q')
		{
			putchar(x)
		}
		x++;
	}
	putchar('\n');
	return (0);
}
